#include<stdio.h>
void main()
{
	int c;
	printf("enter lenth of array\n");
	scanf("%d",&c);

	int d[c];
	for (int i=0;i<c;++i)
	{
		printf("enter element no %d\n",i);
		scanf("%d",&d[i]);
	}
	for (int i = 0; i < count; ++i)
	{
		if(d[i]<d[])
	}
}